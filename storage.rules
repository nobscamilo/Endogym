rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Only the owner of the plates folder can read/write their photos
    match /plates/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
